# Telegram-QuoteBot Config File

# The bot's username and token.  These are generated by Telegram when creating a new bot account through the Botfather.
User: 
Token: 
# A custom status message, to allow users to ping the bot and see if it's alive.
# You may use escape sequences such as \n to break up the text into multiple lines.
StatusMessage: 
# Information needed to connect to the MySQL database.
DB: 
DBuser: 
DBpass: 
# A list of possible tables to store quotes in.  This allows the owner to use multiple different tables in the MySQL database
# for storing different categories of quotes, and will be displayed in the help message so users can know which types of quotes
# they can specify for storage.
Tables: 
